# PRODUCT CATALOG SERVICE 
Приложение для управления товарами, брендами и пользователями.  
Реализовано в рамках домашнего задания.

##Возможности 
- Аутентификация и управление пользователями
- Регистрация, вход, роли: USER, SELLER, ADMIN.
- Управление сущностями
  Товары: добавление, редактирование (только владельцем), удаление, просмотр
  Бренды: создание, просмотр (уникальные, привязаны к пользователю)
  Категории: создание, выбор при добавлении товара
- Поиск и фильтрация
  По названию, бренду, категории, ценовому диапазону с поддержкой комбинированных фильтров.
- Кэширование
  Результаты поиска кэшируются для ускорения повторных запросов.
- Аудит
  Все действия пользователей (вход, регистрация, изменения) записываются в файл audit.log.
- Enterprise-архитектура
  Чёткое разделение на слои:
  in - консольный ввод/вывод
  domain - сущности, сервисы, исключения
  out - репозитории, работа с БД
- Консольное меню с Command Pattern 
  Использован паттерн Command и enum вместо switch-case с магическими строками. (Исправление первого дз)
- Работа с PostgreSQL
  Все данные хранятся в PostgreSQL через чистый JDBC (без Spring).
- Миграции через Liquibase
  Создание структуры БД в схеме marketplace
  Предзаполнение тестовыми данными
  Служебные таблицы Liquibase - в отдельной схеме liquibase
- Docker и Testcontainers
  docker-compose.yml для локального развертывания PostgreSQL с кастомными кредами
- Интеграционные тесты с Testcontainers (запуск PostgreSQL в Docker для тестов)
  Конфигурация через файл
  Подключение к БД и параметры Liquibase настраиваются через application.properties.

##Технологии
- Java 17 (Core)
- PostgreSQL 15
- Liquibase (миграции)
- Testcontainers (интеграционные тесты)
- Maven (сборка и управление зависимостями)
- Docker (контейнеризация БД)
- Lombok (уменьшение шаблонного кода)

##Запуск
- Запустить PostgreSQL в Docker: docker-compose up -d
- Прогнать миграции: mvn liquibase:update
- Запустить приложение

##Комментарий
Поскольку была **творческая свобода**, редактирование товаров решено было сделать “кастомно”.  
Не то чтобы не хватило времени — **не баг, а фича**.  

Аля **«твёрдые цены»** — технология, ушедшая вместе с развалом СССР. 🇷🇺  
> Если ты не являешься владельцем бренда, то не можешь изменять его параметры,  
> но можешь добавить товар этого бренда себе —  
> менять что-то может **только владелец**.
